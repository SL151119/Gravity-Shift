using UnityEngine;

public class RestartObject : MonoBehaviour
{
    //This script is needed just to declare an object for restart if we collide with it
    //For test task this script attached to Earth child object of Earth main object
    //Attach the required collider to the object with this script
}
